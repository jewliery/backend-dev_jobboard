<!DOCTYPE html>
<html lang="en">
  <head>
    <title>JobBoard</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
  </head>

  <body>
    <div class="controls">
      <div class="controls-inner">
        <a>
          <%= inline_svg_tag "job-search.svg", class: 'logo-header' %>
        </a>
        <a <%= link_to "Home", jobs_path %></a>
      </div>
    </div>

    <div class='container'>
      <%= yield %>
    </div>
  </body>
  <%= render "footer" %>
</html>